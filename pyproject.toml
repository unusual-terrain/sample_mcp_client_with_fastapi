[project]
name = "sample-mcp-client-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.52.0",
    "fastapi[standard]>=0.115.12",
    "mcp[cli]>=1.9.1",
]
